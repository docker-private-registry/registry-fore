<nav class="navbar navbar-default">
  <div class="container">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar-collapse" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <i class="fa fa-bars"></i>
      </button>
    </div>

    <div class="navbar-collapse collapse navbar-responsive-collapse">

      {{#if session.isAuthenticated}}

      <ul class="nav navbar-nav">
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">运营相关&nbsp;<span class="caret"></span></a>
          <ul class="dropdown-menu">
            <li>{{#link-to 'accounts'}}运营人员{{/link-to}}</li>
          </ul>
        </li>
      </ul>

      {{/if}}

      <ul class="nav navbar-nav navbar-right">
      {{#if session.isAuthenticated}}
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">
            {{sessionAccount.account.nickname}} <span class="caret"></span>
          </a>

          <ul class="dropdown-menu">
            <li><a {{action 'onLogout'}} >{{t "account.logout"}}</a></li>
          </ul>
        </li>
      {{else}}
        {{#link-to "accounts.login" tagName="li"}}<a href="#">{{t "account.login"}}</a>{{/link-to}}
      {{/if}}
      </ul>
    </div>
  </div>
</nav>

